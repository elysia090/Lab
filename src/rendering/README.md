# Rendering Experiments

This directory contains rendering prototypes together with checked-in output artifacts used as visual references.

- `*.py` files are the source experiments.
- `*.png` and `*.gif` files are generated outputs kept for comparison and documentation.
- Several filenames are historical; new additions should keep code and generated assets clearly paired.
